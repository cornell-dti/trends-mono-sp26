# Lecture 9 Client - Deployment

1. Install packages
    ```bash
    pnpm install
    ```
2. Locally run the web app at http://localhost:5173/
    ```
    pnpm run dev
    ```
3. Deploy on Vercel with set environment variables as per local ignored `.env`
4. Update Firebase allowed domains to include the Vercel domain